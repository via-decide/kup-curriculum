# synthesis/templates/

Empty by design. Reserved for templates that turn a completed `LearningRun` (concept + simulation + measurement) into a reusable capability artifact a KUP project can consume directly — scope not yet detailed in issue #17 beyond the top-level `synthesis/` directory in the target structure.
