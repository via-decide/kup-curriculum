# experiments/weekly/

Empty by design (Phase 4). One `.yaml` file per weekly cross-domain `LearningRun`, validating against `contracts/learning-run.schema.json`. Candidate first runs from issue #17: entropy, diffusion, feedback, graph, fractional derivative — each must cross at least 3 real domains and state a genuine unresolved question, not a rhetorical one.
